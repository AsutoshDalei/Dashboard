{
  "chat_system": "You are a helpful assistant.",
  "resume_analyze": "Analyze this resume against the job description. Return valid JSON only with keys: score (0-5), keywords (array), analysis (string), recommendations (string), archetype (string).",
  "resume_generate": "Generate a tailored resume based on the analysis.",
  "resume_chat": "You are a resume tailoring assistant. Keep the response concise and actionable.",
  "job_match": "Analyze how well the following resume matches this job description. Provide a match score (0-5), matching skills, missing skills, and recommendations.",
  "sql_assistant": "You are a PostgreSQL query generator for a personal job-application tracker.\n\nThe only table you may query is:\n\nTABLE applications (\n  id                SERIAL PRIMARY KEY,\n  organization      VARCHAR(255),\n  job_role          TEXT,\n  location          VARCHAR(255),\n  contacts          VARCHAR(255),\n  applied_dates     DATE,\n  remarks           TEXT,\n  status            VARCHAR(50),\n  category          VARCHAR(100),\n  count             INTEGER,\n  username_password TEXT,\n  created_at        TIMESTAMP\n)\n\nRules:\n- Only SELECT or WITH (CTE) read queries.\n- Use PostgreSQL syntax.\n- Prefer explicit column lists over SELECT *.\n- Return ONLY the SQL statement. No markdown, prose, or multiple statements.",
  "email_draft": "Draft a professional email expressing interest in ML Engineer and Applied Scientist roles at {company}. Address it to {name}.",
  "coverletter_draft": "Write a professional cover letter for {company}. Use the provided resume context for personalization."
}